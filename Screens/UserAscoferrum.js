import * as React from 'react'
import {Text, View, StyleSheet, TouchableOpacity, Image, Linking, ScrollView} from 'react-native'

export default class UserAscoferrum extends React.Component{
    render(){
        return(
            <View>
                <ScrollView style={{width:'100%'}}>
                    
                </ScrollView>
            </View>
        )
    }
}